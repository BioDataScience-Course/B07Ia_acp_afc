---
title: "___"
author: "___"
date: "___"
output: html_document
---

<!--% En partant du jeu de données `crabs` du package `MASS`, réalisez une ACP afin de différencier les quatre groupes de *Leptograpsus variegatus* (deux espèces et les deux sexes pour chaque espèce). -->

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
# Packages utiles

```

# Introduction

<!--% Rédigez une introduction de 5 phrases maximum sur *Leptograpsus variegatus* (Fabricius, 1793). -->

# Analyse

<!--% Importez le jeu de données `crabs` du package `MASS`. -->

```{r import}
crabs <- ___
```

## Exploration des données

<!--% Réalisez une matrice de nuages de points pour vérifier la linéarité entre les variables quantitatives intéressantes. -->

```{r scatmat}

```

<!--% Interprétez la matrice de nuages de points en 3 phrases maximum. -->

<!--% Réalisez un graphique de la matrice de corrélation de Pearson. -->

```{r corrplot}

```

<!--% Interprétez la matrice de corrélation en 3 phrases.-->

## ACP sur les variables brutes

<!--% Réalisez une ACP avec la fonction `pca()`. Nommez cet objet `crabs_pca`. Affichez également le résumé de cet objet. Utilisez l'ensemble des variables quantitatives à disposition. -->

```{r pca}

```

<!--% Réalisez le graphique des éboulis de l'objet `crabs_pca`.-->

```{r pca_scree}

```

<!--% Interprétez le résumé de l'ACP et le graphique des éboulis en 5 phrases. Ayez une réflexion également concernant la part de variance exprimée sur le premier axe de l'ACP. -->

## ACP sur des indices calculés

<!--% Calculer des indices qui permettent de résoudre le problème observé ci-dessus. Préparez un jeu de données `crabs2` en divisant chacune des variables de `crabs` par `length`. -->

```{r crabs2}

```

<!--% Réalisez une seconde ACP avec la fonction `pca()` sur `crabs2`. Nommez cet objet `crabs_pca2`. Affichez également le résumé de cet objet. Utilisez l'ensemble des variables disponibles et pertinentes pour cette analyse. -->

```{r pca2}
crabs_pca2 <- pca(___)
```

<!--% Réalisez le graphique des éboulis sur l'objet `crabs_pca2`.-->

```{r pca2_scree}

```

```{=html}
<!--% Interprétez le résumé de l'ACP et le graphique des éboulis en 5 phrases.


<!--% Réalisez un graphique permettant de visualiser la représentation des variables dans le premier plan de l'ACP. -->
```
```{r pca2_loadings}

```

<!--% Interprétez en 3 phrases ce graphique.-->

<!--% Calculez une nouvelle variable dans le jeu de données `crabs2` qui combine la variable `species` et la variable `sex` en une seule. Nommez cette nouvelle variable `sp_sex`. -->

```{r new_var}
crabs2 <- smutate(___, sp_sex = paste(___))
```

<!--% Réalisez un graphique permettant de visualiser la représentation des individus dans le premier plan de l'ACP. Utilisez la variable `sp_sex` afin de différencier les individus sur votre graphique par la couleur. -->

```{r pca2_scores}

```

<!--% Interprétez en 5 phrases ce graphique.-->

# Conclusion

<!--% Concluez ce carnet de notes en 5 phrases maximum. -->
